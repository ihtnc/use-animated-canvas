import type { MenuItem } from '@/app/usage'

const item: MenuItem = {
  label: 'Basic Render',
  href: '/usage/basic',
  description: 'Basic rendering example',
  category: 'Basic Usage',
  tags: ['basic']
}

export default item