import type { MenuItem } from '@/app/usage'

const item: MenuItem = {
  label: 'Render Data',
  href: '/usage/data',
  description: 'Basic rendering with data example',
  category: 'Basic Usage',
  tags: ['basic', 'render', 'data']
}

export default item