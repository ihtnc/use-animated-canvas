import type { MenuItem } from '@/app/usage'

const item: MenuItem = {
  label: 'Render Data',
  href: '/usage/data',
  description: 'Render using custom data',
  category: 'Basic Usage',
  tags: ['data']
}

export default item